# @ziply/cli

Small CLI for Ziply.

```sh
ziply create hello.txt -o archive.zip
ziply add archive.zip upload.yml ./upload.yml -o next.zip
ziply list next.zip
```

Install:

```sh
npm install -g @ziply/cli
```

Full docs and roadmap: https://github.com/eklavya-raj/ziply
